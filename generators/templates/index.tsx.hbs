import * as S from './styles'

const {{pascalCase name}} = (): React.ReactElement => {
  return (
    <S.{{pascalCase name}}>
      {{lowerCase name}}
    </S.{{pascalCase name}}>
  )
}

export default {{pascalCase name}}
