import { renderWithTheme } from 'utils/render-with-theme'

import {{pascalCase name}} from 'components/{{lowerCase path}}/{{pascalCase name}}'

describe('<{{pascalCase name}} />', () => {
  it('', () => {
    expect(1).toBe(1)
  })
})
