import { renderWithTheme } from 'utils/render-with-theme'

import {{pascalCase name}} from '..'

describe('<{{pascalCase name}} />', () => {
  it('render {{pascalCase name}}', () => {
    renderWithTheme(<{{pascalCase name}} />)
  })
})
